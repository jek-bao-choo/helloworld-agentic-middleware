# llm_prompt.py
"""
Stores predefined prompts for the agentic middleware.
"""

# For this simple case, we only have one prompt.
INSTALL_CURL_MACOS = "Q1) When is your knowledge cutoff? Q2) What is command to install curl in MacOS? Keep it short."

# Export the specific prompt we'll use for now
default_prompt = INSTALL_CURL_MACOS
