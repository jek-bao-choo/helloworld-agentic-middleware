# helloworld-click-client
 
```bash
uv init

uv add click litellm

uv run main.py --product "some query" --operation "ask" --target openrouter 

uv run main.py --product "some query" --operation "ask" --target local 

uv run main.py --product "some query" --operation "ask"
```
